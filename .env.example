# Google Safe Browsing Lookup API v4
GOOGLE_SAFE_BROWSING_API_KEY=

# VirusTotal API v3
VIRUSTOTAL_API_KEY=

TRANCO_LIST_PATH=tranco_top1m.csv

# Directories (defaults are fine for most setups)
# SCREENSHOT_DIR=screenshots
# DOWNLOAD_DIR=downloads
# MODELS_DIR=models

# Deployment naming (single source of truth for local deploy scripts)
GCP_PROJECT_ID=url-scanning-api
GCP_REGION=europe-west1
SERVICE_NAME=url-scanning-api
STAGING_SERVICE_NAME=url-scanning-api-staging
AR_REPO=url-scanning-api
IMAGE_NAME=url-scanning-api
MODEL_BUCKET=url-scanning-api-models-bucket
MODEL_ARTIFACTS_PREFIX=url-scanning-api-models
GHA_SA_NAME=gha-${GCP_PROJECT_ID}
REPO_SLUG=your-org/your-repo
